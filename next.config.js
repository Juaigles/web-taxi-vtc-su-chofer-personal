/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com'], // Añade aquí otros dominios si necesitas
    },
   
  };
  

  

module.exports = nextConfig
